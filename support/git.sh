#!/bin/bash
echo "
Useful git commands:
  Current status: git status
  Revert to current state of remote: git checkout .; git pull
  Find differences from what's committed: git diff
  Add all new files: git add --all
  Commit all changes: git commit -a
  Push changes: git push; git fetch # Bring local git up to date with remote
"	
